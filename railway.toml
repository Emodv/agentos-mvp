[build]
builder = "dockerfile"

[deploy]
startCommand = "./proxy"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
REDIS_URL = { value = "${REDIS_URL}" }
L2AGENT_API_KEY = { value = "${L2AGENT_API_KEY}" }
